target(lang: golang)

attrs (
    "rpl:grpc"
)

@grpc()
@grpc.Model()
@grpc(subject: "model")
model User {
    Id int
    {
        @grpc.id()
    }

    Name string

    func Slug return (string)
    func Greeting (prefix string) return (string)
}
