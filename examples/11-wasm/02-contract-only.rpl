target(lang: ffi)

attrs (
    "rpl:wasm"
)

@wasm(
    wit: "acme:calculator@0.2.0",
    guest: "none",
    hosts: "none",
)
model Calculator {
    Name string

    func Add (left int64, right int64) return (int64)
    func Divide (left float64, right float64) return (float64, error)
}
